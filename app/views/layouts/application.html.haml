!!!
%html
   %head
      - if @title.blank?
         %title LebGeeks Jobs
      - else
         %title= @title
      = stylesheet_link_tag :all 
      = javascript_include_tag 'jquery.min'
      = javascript_include_tag 'rails'
      = csrf_meta_tag 
   %body
      = yield 